/**
 * Place your enum files in this folder.
 *
 * Enums define a set of named constants, making your code more
 * readable and less error-prone than using raw strings or numbers.
 *
 * Examples:
 *   - user-role.enum.ts   → UserRole.Admin, UserRole.Editor, UserRole.Viewer
 *   - status.enum.ts      → Status.Active, Status.Inactive, Status.Pending
 *   - http-status.enum.ts → HttpStatus.Ok, HttpStatus.NotFound, HttpStatus.ServerError
 *
 * Usage:
 *   export enum UserRole {
 *     Admin = 'ADMIN',
 *     Editor = 'EDITOR',
 *     Viewer = 'VIEWER',
 *   }
 *
 * You can safely delete this file once you've added your own.
 */
