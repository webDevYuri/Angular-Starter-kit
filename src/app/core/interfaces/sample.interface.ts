/**
 * Place your interface files in this folder.
 *
 * Interfaces define the shape of objects and API responses,
 * providing type safety and auto-completion throughout your app.
 *
 * Examples:
 *   - user.interface.ts         → IUser, IUserProfile, IUserCredentials
 *   - api-response.interface.ts → IApiResponse<T>, IPaginatedResponse<T>
 *   - table.interface.ts        → ITableColumn, ITableConfig
 *
 * Usage:
 *   export interface IApiResponse<T> {
 *     data: T;
 *     message: string;
 *     success: boolean;
 *   }
 *
 *   export interface IPaginatedResponse<T> extends IApiResponse<T[]> {
 *     total: number;
 *     page: number;
 *     pageSize: number;
 *   }
 *
 * You can safely delete this file once you've added your own.
 */
